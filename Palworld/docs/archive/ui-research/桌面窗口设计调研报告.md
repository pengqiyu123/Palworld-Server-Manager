# 桌面级应用窗口设计与标题栏交互失效根因调研

**日期**：2026-07-21
**执行模式**：完整（研究团队协作产出，纯调研、未改动任何代码）

---

## 目录

- [1. 桌面窗口的逻辑结构与种类划分](#1-桌面窗口的逻辑结构与种类划分)
- [2. 标题栏的构造与 Win32 命中测试机制](#2-标题栏的构造与-win32-命中测试机制)
- [3. 无边框透明窗交互失效的三层根因](#3-无边框透明窗交互失效的三层根因)
- [4. 桌面窗口定制化的正确设计方向](#4-桌面窗口定制化的正确设计方向)
- [结论](#结论)
- [参考文献](#参考文献)

---

## 引言

桌面级应用的"窗口"并非一块简单的矩形画布。在 Windows 平台上，自 Vista 起每个窗口由桌面窗口管理器（DWM）统一合成——窗口先绘制到离屏表面，再作为独立纹理经 GPU 做 Alpha 混合（[DWM Performance Best Practices — Microsoft Learn](https://learn.microsoft.com/en-us/windows/desktop/dwm/bestpractices-ovw)）。窗口在逻辑上分为系统管理的**非客户区（NC，含标题栏、边框、三按钮，负责命中测试）**与应用自管的**客户区**（[Title bar customization — Microsoft Learn](https://learn.microsoft.com/windows/apps/develop/title-bar)）。围绕"窗口关系 / 文档模型 / 边框装饰 / 标题栏 / 透明度"五个维度，桌面窗口可划分出多种类型（[Window Styles — Microsoft Learn](https://learn.microsoft.com/windows/win32/winmsg/window-styles)）。

窗口顶部的操作栏（标题栏）常被误认为应用自己画的一条控件条，但事实相反：标题栏是系统托管的非客户区，其右侧最小化/最大化/关闭三按钮是系统"预约"并持续控制的热区，而非普通按钮（[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。它们的可拖拽、可点击，来自 Win32 命中测试机制（WM_NCHITTEST）：系统据鼠标坐标返回 `HTCAPTION(=2)` / `HTMINBUTTON(=8)` / `HTMAXBUTTON(=9)` / `HTCLOSE(=20)`，再统一执行拖拽与关闭等非客户区操作（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)）。换言之，拖拽与三按钮是**系统能力**，而非应用逻辑。

本项目（Tauri2 + `decorations:false` + `transparent:true` 无边框透明窗）被判定为"负优化"，正是因为在关闭系统 NC 区后，这套系统能力从源头消失，而应用层既未充分重建命中路由，又因 Tauri v2 权限缺失与分层窗材质限制，导致窗口拖不动、三按钮点不到（[Tauri v2 Window Customization](https://v2.tauri.app/learn/window-customization/)）。本报告沿"结构 → 机制 → 根因 → 方向"四章，系统回答两个问题：桌面窗口的结构与种类，以及标题栏为何失效、正确做法是什么。

---

## 1. 桌面窗口的逻辑结构与种类划分

### 一、论点

桌面级应用窗口，本质上是操作系统为应用程序分配的一块**矩形绘图与交互容器**：它既是用户看到的视觉边界，也是接收鼠标、键盘、触摸等输入的区域。在 Windows 平台上，自 Vista 起，窗口不再由程序直接绘制到屏幕，而是由**桌面窗口管理器（DWM）统一合成**——每个窗口先绘制到离屏表面，再作为独立的纹理经由 GPU 进行 Alpha 混合，最终合成为桌面图像（[DWM Performance Best Practices — Microsoft Learn](https://learn.microsoft.com/en-us/windows/desktop/dwm/bestpractices-ovw)；[The Desktop Window Manager — Microsoft Learn](https://learn.microsoft.com/en-us/windows/win32/learnwin32/the-desktop-window-manager)）。在这一机制下，窗口在逻辑上被划分为**非客户区（Non-client Area, NC 区）**与**客户区（Client Area）**两大部分，其外观由一组窗口样式（Window Styles）决定；而围绕"关系、文档模型、边框装饰、标题栏、透明度"五个维度，桌面窗口可进一步划分为多种类型。

### 二、论据

**1. 窗口的定义与 DWM 合成机制**

数据显示，在启用 DWM 合成后，**被遮挡的窗口不再收到重绘消息，因为其内容已作为可合成的纹理存在**（[DWM Performance Best Practices](https://learn.microsoft.com/en-us/windows/desktop/dwm/bestpractices-ovw)）。DWM 基于 DirectX/WDDM，利用 GPU 硬件加速完成合成与渲染，窗口内容被转换为 Direct3D 纹理后映射到屏幕（[The Desktop Window Manager](https://learn.microsoft.com/en-us/windows/win32/learnwin32/the-desktop-window-manager)）。这带来半透明、模糊、实时缩略图等视觉效果，也意味着**窗口的"矩形外观"是合成层的结果，而非单一窗体内部独立绘制**。

**2. 逻辑结构：非客户区（NC）vs 客户区**

窗口在逻辑上分为两部分：
- **非客户区（NC 区）**：由系统绘制与管理的边框、标题栏、系统菜单、最小化/最大化/关闭三按钮等"窗口装饰（window chrome）"。系统在此区域负责**命中测试（hit-testing）**——即判断鼠标落在标题栏（可拖动）、边框（可缩放）还是按钮（可关闭）（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)；[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。
- **客户区（Client Area）**：应用实际绘制内容、承载业务 UI 的区域。

在 WinUI 3 / Windows App SDK 的体系里，一个现代桌面应用窗口由 `AppWindow`（基于 Win32 HWND）承载，应用内容通过 XAML 岛延伸进窗口；当设置 `ExtendsContentIntoTitleBar = true` 时，客户区会覆盖原 NC 区，但**系统仍垄断右上角 caption buttons 的绘制与输入**（[Structure a modern WinUI 3 desktop app](https://learn.microsoft.com/zh-hk/windows/apps/develop/ui/windows-app-sdk-app-structure)；[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。这说明：**NC 区与系统命中测试的绑定，是窗口"可拖动、可关闭"能力的根本来源**。

**3. 关键窗口样式**

窗口的外观由创建时指定的样式位决定（多数创建后不可改）。根据微软官方文档（[Window Styles](https://learn.microsoft.com/windows/win32/winmsg/window-styles)）：
- **WS_OVERLAPPEDWINDOW** = WS_OVERLAPPED | WS_CAPTION | WS_SYSMENU | WS_THICKFRAME | WS_MINIMIZEBOX | WS_MAXIMIZEBOX，即带标题栏、系统菜单、可调边框、最小化/最大化按钮的"标准重叠窗口"。
- **WS_THICKFRAME**（同 WS_SIZEBOX）：可调边框；**WS_SYSMENU**：标题栏上的窗口菜单；**WS_POPUP**：弹出式窗口（不可与 WS_CHILD 共用）。

扩展样式方面（[Extended Window Styles](https://learn.microsoft.com/ja-jp/windows/win32/winmsg/extended-window-styles)）：
- **WS_EX_LAYERED**：分层窗口，可实现每像素 Alpha 透明度；DWM 文档还提示，顶层 `WS_EX_TRANSPARENT` 窗口应配合 `WS_EX_LAYERED` 以正确进行命中测试（[DWM Performance Best Practices](https://learn.microsoft.com/en-us/windows/desktop/dwm/bestpractices-ovw)）。
- **WS_EX_TOOLWINDOW**：创建工具窗口（不显示在任务栏、标题栏较小）。

**4. 种类划分（按五个维度）**

| 维度 | 类别 | 说明 |
|------|------|------|
| 窗口关系 | 主窗口 / 子窗口（WS_CHILD）/ 对话框（WS_DLGFRAME）/ 工具窗口（WS_EX_TOOLWINDOW）/ 浮动窗口 | 从属与层级关系 |
| 文档模型 | SDI（单文档）/ MDI（多文档）/ 文档型多标签（如浏览器） | 一次承载多少文档 |
| 边框装饰 | 原生带边框窗口 vs 无边框窗口（frameless, `decorations:false`） | 系统是否绘制 chrome |
| 标题栏 | 原生系统标题栏 vs 自绘/自定义标题栏 | 系统还是应用负责绘制与命中 |
| 透明度 | 普通窗口 vs 分层窗口（WS_EX_LAYERED） | 是否每像素 Alpha |

### 三、分析

五个维度的分类并非正交，而是层层叠加：一个典型无边框窗口，往往同时是"主窗口 + SDI + frameless + 自定义标题栏 +（可能）分层透明"的组合。值得注意的是，**无边框 + 透明 的组合，直接取消了系统对非客户区的绘制与命中测试责任**——标题栏拖动、三按钮点击等能力被转嫁给应用层实现。业界观点认为，对于大多数业务型桌面应用，这种"移除原生 chrome 再自行重建"的做法往往得不偿失：它放弃了系统免费提供的稳定性与无障碍支持，却只换来有限的视觉统一（[Tauri 自定义 Titlebar 与窗体圆角实现指南](https://garden.piemoe.com/dev/8zoti5ag) 亦提示，macOS 上自定义标题栏会丢失系统提供的移动/对齐窗口等功能）。这构成了本项目中"Tauri2 + decorations:false + transparent:true 无边框透明窗被判定为负优化"的结构性背景。

### 四、小结

本章厘清：桌面窗口是操作系统经 DWM 合成的矩形交互容器，逻辑上由系统管理的非客户区（含命中测试）与应用自管的客户区构成，其外观由窗口样式位决定；并沿"窗口关系 / 文档模型 / 边框装饰 / 标题栏 / 透明度"五个维度，建立了桌面窗口的分类框架。

---

## 2. 标题栏的构造与 Win32 命中测试机制

### 论点

桌面应用窗口在视觉上由"客户区"（应用自行绘制与交互的内容区）和"非客户区（NC 区）"（标题栏、边框、系统菜单等由系统绘制与管理的部分）两部分构成（[Window Styles](https://learn.microsoft.com/windows/win32/winmsg/window-styles)）。操作栏（标题栏）本身并非应用自己画的一条普通控件条，而是系统托管的非客户区；其右侧的最小化、最大化/还原、关闭三个按钮，本质上是系统在该非客户区内"预约"并持续控制的热区，而非应用自行绘制的普通按钮控件（[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。本章核心论点：窗口的拖拽移动与三按钮点击，是 Windows 通过"命中测试（hit-test）+ 非客户区消息"统一提供的**系统能力**，而非应用逻辑；一旦关闭系统 NC 区（如无边框窗），这些能力便从源头消失。

### 论据

**1. 标题栏属于系统绘制的非客户区。** 窗口外观由窗口样式（Window Styles）决定。`WS_CAPTION` 赋予窗口标题栏，`WS_OVERLAPPEDWINDOW` 是 `WS_OVERLAPPED | WS_CAPTION | WS_SYSMENU | WS_THICKFRAME | WS_MINIMIZEBOX | WS_MAXIMIZEBOX` 的组合，即一个标准重叠窗口同时拥有标题栏、系统菜单、可缩放边框与三按钮（[Window Styles](https://learn.microsoft.com/windows/win32/winmsg/window-styles)）。标题栏与边框都属于"非客户区"，其绘制、拖拽、缩放等基础行为由系统负责，应用默认无需、也通常不直接实现这些逻辑（[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。

**2. 三按钮是系统"预约"的热区，不是普通控件。** Microsoft 的标题栏定制文档明确指出：默认标题栏包含标题文本、系统图标/菜单，以及最小化、最大化/还原、关闭按钮（caption controls）；系统始终保留对标题栏右上角（或 RTL 语言下左上角）caption 按钮区域的**控制权**，以保证窗口的最小功能（拖动、最小化、最大化、关闭）始终可用（[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。换言之，这三个按钮不是应用往标题栏上摆的 Button 控件，而是系统在自己的非客户区里预留、绘制并处理的"热区"。

**3. Win32 命中测试机制（WM_NCHITTEST）。** 每次发生鼠标事件，系统都会向包含光标热点的窗口发送 `WM_NCHITTEST` 消息，其 `lParam` 携带光标的屏幕坐标；`DefWindowProc` 据此坐标返回"命中测试值"，系统再用该值判断接下来投递"客户区消息"还是"非客户区消息"（[About mouse input: WM_NCHITTEST](https://learn.microsoft.com/en-us/windows/win32/inputdev/about-mouse-input)）。常见命中常量如下：热点在客户区返回 `HTCLIENT(=1)`；在标题栏返回 `HTCAPTION(=2)`；在最小化按钮返回 `HTMINBUTTON(=8)`；在最大化按钮返回 `HTMAXBUTTON(=9)`；在关闭按钮返回 `HTCLOSE(=20)`；在边框分别返回 `HTLEFT(=10)`、`HTRIGHT(=11)`、`HTTOP(=12)`、`HTBOTTOM(=15)`（连同四角 `HTTOPLEFT` 等）（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)）。

**4. 命中值决定后续非客户区消息与系统行为。** 若 `WM_NCHITTEST` 返回 `HTCLIENT`，系统将屏幕坐标转换为客户区坐标并投递客户区鼠标消息（如 `WM_LBUTTONDOWN`），交由应用处理；若返回的是其他非客户区值，系统则投递带 `NC` 前缀的非客户区消息（如 `WM_NCLBUTTONDOWN`），并把命中值放入消息的 `wParam`（[About mouse input](https://learn.microsoft.com/en-us/windows/win32/inputdev/about-mouse-input)）。当窗口过程（或 `DefWindowProc` 默认处理）对标题栏按下返回 `HTCAPTION` 时，系统会将窗口标记为"拖拽状态"，在鼠标松开前持续跟踪鼠标并同步移动窗口；对 `HTMINBUTTON`/`HTMAXBUTTON`/`HTCLOSE` 则由系统默认执行最小化、最大化、关闭操作（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)）。

### 分析

上述机制揭示了一个关键结论：**窗口的拖拽移动、边框缩放、三按钮操作，都不是应用自己实现的交互逻辑，而是由系统通过"命中测试 + 非客户区消息（WM_NCLBUTTONDOWN 等）"统一处理的"系统能力"。** 应用正常只需负责 `HTCLIENT` 客户区内的交互；标题栏与三按钮的"可点、可拖、可关"来自系统对非客户区的命中分发。

这也直接解释了"交互失效"的根因：当采用 `decorations:false`（Tauri）或 `frame:false`（Electron）关闭系统 NC 区、创建无边框窗时，系统不再生成标题栏与 caption 按钮，原本在非客户区里的标题栏/三按钮命中区域不复存在——于是 `DefWindowProc` 再也收不到 `HTCAPTION`、`HTMINBUTTON`、`HTCLOSE` 之类的命中值，标题栏拖拽与三按钮点击便"从源头消失"。Electron 的官方文档佐证了这一点：frameless 窗口默认**不可拖拽**，原生 min/max/close 也不再可用，应用必须自行用 CSS `-webkit-app-region: drag` 标定可拖区域、并自行接管按钮逻辑（[Frameless Window — Electron](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）。同时，透明无边框窗在 Windows 上还依赖 DWM 启用、本身不可缩放、点击穿透受限，且会丢失原生窗口阴影与 Aero Snap 吸附等体验（[Frameless Window](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）。

### 小结

- 标题栏是系统绘制的非客户区；最小化/最大化/关闭是系统在该区"预约"并持续控制的热区，而非应用普通按钮控件。
- 拖拽移动、边框缩放、三按钮操作均为系统"命中测试 + 非客户区消息"提供的系统能力，应用默认只负责客户区。
- 关闭系统 NC 区（无边框窗）后，相关命中区域从源头消失，导致标题栏拖拽与三按钮点击整体失效。

| 命中值 | 数值 | 系统行为 |
|--------|------|----------|
| HTCLIENT | 1 | 客户区，投递 WM_LBUTTONDOWN 等 |
| HTCAPTION | 2 | 标题栏，系统非客户区拖拽移动 |
| HTMINBUTTON | 8 | 最小化按钮，系统执行最小化 |
| HTMAXBUTTON | 9 | 最大化按钮，系统执行最大化/还原 |
| HTLEFT/RIGHT/TOP/BOTTOM | 10/11/12/15 | 边框，系统缩放 |
| HTCLOSE | 20 | 关闭按钮，系统执行关闭 |

---

## 3. 无边框透明窗交互失效的三层根因

### 论点：这不是 bug，是"系统交互基础设施被关掉"

本项目（Tauri v2 + `decorations:false` + `transparent:true`）被判定为"负优化"，核心症结在于：开发者用一行配置关掉了操作系统为窗口提供的整套交互基础设施，却没有（或没能力）用应用层等价地重建它。事实层面，窗口的拖动与三按钮，从来不是应用自己绘制的像素，而是 Windows 非客户区（NC 区）由系统统一管理的命中热区；一旦无边框 + 透明，这套热区被整体移除，拖不动、点不到就成了必然结果，而非偶发缺陷。本章将这一失效拆解为三层根因。

### 论据一（根因①）：`decorations:false` 整体移除系统 NC 区

在 Win32 模型里，鼠标位置由 `WM_NCHITTEST` 消息判定，系统据此把不同区域映射为命中常量：`HTCAPTION`(2) 表示标题栏可拖动、`HTMINBUTTON`(8)/`HTMAXBUTTON`(9)/`HTCLOSE`(20) 分别对应最小化/最大化/关闭按钮，`HTCLIENT`(1) 为客户区（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)）。当这些常量返回对应 NC 值时，系统会自动完成拖动、缩放、关闭等窗口管理操作，无需应用干预。

而 Tauri 的 `decorations:false`（[Tauri v2 Window Customization](https://v2.tauri.app/learn/window-customization/)）等价于让系统不再绘制原生标题栏与边框，即把承载上述所有热区的 NC 区从窗口上"摘掉"；Electron 的 `frame:false` 同理，官方明确其为 "a window that has no chrome"——工具栏、边框等系统图形元素全部消失（[Electron Frameless Window](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）。因此，拖拽失效与三按钮失灵，本质是"功能被关闭"，是配置的直接语义后果，而非框架缺陷。

### 论据二（根因②）：命中测试改由应用自管，配置失误即吞掉点击

无边框下，应用必须"骗"系统，用客户区模拟 NC 行为。两套主流方案：Tauri 通过给元素加 `data-tauri-drag-region` 属性，将其声明的区域在命中测试中映射为 `HTCAPTION`，从而恢复拖动（[Tauri v2 Window Customization](https://v2.tauri.app/learn/window-customization/)）；Electron 则靠 CSS `-webkit-app-region: drag` 标记可拖拽区，并用 `-webkit-app-region: no-drag` 把按钮排除出拖拽区（[Electron Frameless Window](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）。

Electron 官方特别警告：若整窗设为可拖拽却未把按钮标记为 `no-drag`，"it would be impossible for users to click on them"——点击事件会被拖拽手势吞掉。这正是本项目上一轮出问题的叠加点：删掉了 `data-tauri-drag-region`、改用 `startDragging()` 自管拖拽，却未同步保证按钮区域的命中排除，于是"拖拽区"与"按钮区"命中路由冲突，三按钮点不到（[Tauri v2 拖拽失效修复](https://blog.csdn.net/2201_75395601/article/details/156907820)）。

### 论据三（根因③）：Tauri v2 需显式授权 + 透明窗沦为分层窗

根因③ 有两个独立子项。其一是权限：Tauri v2 对窗口拖拽引入了"权限 + 区域"双重限制——必须在 capabilities 中显式启用 `core:window:allow-start-dragging`，否则 `startDragging()` 会静默失败或不报错但无效果（[Tauri v2 Window Customization](https://v2.tauri.app/learn/window-customization/)；[Tauri v2 拖拽失效修复](https://blog.csdn.net/2201_75395601/article/details/156907820)）。有开发者复盘指出，缺权限时"调用 appWindow.startDragging() 也没效果，甚至报权限相关错误"——这恰好解释了本项目改用 `startDragging` 后"依然拖不动"的现象。

其二是材质与系统动画的丧失：`transparent:true` 在 Windows 上通常创建分层窗（`WS_EX_LAYERED`），整窗由 WebView 叠加层承载（[Transparent Drag-and-Drop Shelf (Tauri)](https://dev.to/devrayat000/building-a-transparent-drag-and-drop-shelf-for-windows-with-tauri-the-engineering-war-stories-3c8c)）。此时 CSS 的 `backdrop-filter`/模糊只是 Web 层滤镜，无法作用于窗口下方的真实桌面内容。系统原生的 Mica（Win11 不透明基材）、Acrylic（半透明磨砂）都是 DWM 合成器在窗口基底层施加的效果（[Materials in Windows — Mica/Acrylic](https://learn.microsoft.com/EN-US/windows/apps/design/signature-experiences/materials)），必须经 `window-vibrancy` 这类 crate 调用原生 `DwmSetWindowAttribute` 才能拿到，且 `apply_mica` 仅支持 Win11、`apply_acrylic` 在 Win10/11 拖动时性能下降（[window-vibrancy](https://github.com/tauri-apps/window-vibrancy/)）。更要紧的是，透明/分层窗还会丢失系统阴影、Aero Snap 贴边动画——Electron 官方即载明透明窗 "native window shadow will not be shown" 且 "blur filter only applies to the web page"（[Electron Frameless Window](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）。

### 小结：负优化的本质

综上，透明全窗 + 自绘玻璃表面在视觉上"好看"，实则是用应用层的一层 Web 画布，替换掉了系统提供的整套窗口交互基础设施：NC 命中测试、拖拽热区、三按钮路由、原生材质、窗口阴影与 Aero Snap。三层根因环环相扣：①关掉 NC 区（系统热区消失）→ ②必须自管命中测试（漏标/误配即失效，本项目还删属性改 API 叠加恶化）→ ③还需补权限授权、且透明窗天然拿不到原生材质与系统动画。

业内观点普遍认为，无边框透明窗本身并非不可为，但其成本被严重低估：它把"免费获得"的系统窗口行为变成了"逐项自费重建"的工程负担；当体验未优于原生、却丢了原生全部福利时，"负优化"的判定便成立。

---

## 4. 桌面窗口定制化的正确设计方向

### 论点

无边框透明窗（`decorations:false` + `transparent:true` + CSS 假玻璃）之所以被判定为"负优化"，根源在于它绕开了系统窗口管理器（DWM / Aqua）免费提供的一整套能力——阴影、圆角、缩放、标题栏命中、焦点态——转而用前端自绘去"重造轮子"，结果是交互失效且材质虚假。正确方向并非"彻底舍弃系统 chrome"，而是"在保留系统窗口能力的前提下做可控定制"：优先采用原生材质与系统标题栏机制，仅在必要时把内容扩展到标题栏区域。

### 论据

**1. 微软官方：扩展内容，而非抹除标题栏**

微软在 WinUI 3 现代桌面架构中明确推荐"内容扩展到标题栏"模式——设置 `ExtendsContentIntoTitleBar = true`，再用 `SetTitleBar(UIElement)` 指定可拖拽的标题区域，系统的最小化/最大化/关闭（caption）按钮仍由系统绘制并保留完整命中（[Structure a modern WinUI 3 desktop app](https://learn.microsoft.com/zh-hk/windows/apps/develop/ui/windows-app-sdk-app-structure)）。微软同时提醒：扩展内容后必须为系统 caption 按钮区留出空间（默认右侧约 188px，需按 DPI 用 `RightInset`/`LeftInset` 动态处理），否则内容会盖住三按钮——这正是本项目"三按钮无法点击"的同源陷阱。

**2. 材质分工：Mica 作主窗，Acrylic 仅作浮层**

微软官方材质指南对两种基础材质做了清晰分工：Mica 是 Windows 11 引入的不透明材质，"通过提取用户桌面壁纸色彩做静态着色"，且内置窗口聚焦（active/inactive）状态；Acrylic 是半透明磨砂玻璃，但官方明确"只用于 flyout、上下文菜单等瞬时、轻量消失的浮层"（[Materials in Windows](https://learn.microsoft.com/EN-US/windows/apps/design/signature-experiences/materials)）。第三方渲染效率分析佐证：Mica 因不做实时像素模糊、仅做静态色彩采样，被评价为"近乎零额外性能开销"；而 Acrylic 是"动态高斯模糊、随底层滚动实时重绘、GPU 占用显著"（[Windows 视觉层级与渲染效率](https://tsight.io/articles/16268961)）。在 WinUI 3 中启用 Mica 仅需一行 XAML 的 MicaBackdrop，无需代码，且页面不应设置不透明背景以让材质透出（[同前 WinUI 3 架构](https://learn.microsoft.com/zh-hk/windows/apps/develop/ui/windows-app-sdk-app-structure)）。

**3. Tauri 侧：window-vibrancy 取的是"真系统材质"**

若本项目确要系统毛玻璃质感，正确做法是用 `window-vibrancy` crate：`apply_mica`（Win11）/ `apply_acrylic` 直接调用 DWM API，得到与原生一致的真实系统材质，且不会像裸 `transparent:true` 那样抹掉窗口阴影与缩放能力（[window-vibrancy](https://github.com/tauri-apps/window-vibrancy/)）。但官方 README 标注了性能警告：`apply_acrylic` 在 Win10 v1903+ 与 Win11 build 22000+ 上"拖拽/缩放时性能差"，`apply_blur` 在 Win11 22621+ 同样有此问题，唯 `apply_mica` 在 Win11 无此警告（[同前](https://github.com/tauri-apps/window-vibrancy/)）。社区实践亦记录：Win11 下 acrylic 拖动卡顿，需"拖动时先清除 acrylic、结束再 apply 回来"作 workaround（[Tauri 框架使用 Acrylic 特效拖动卡顿的解决方案](https://juejin.cn/post/7491240940126175284)）；另有开发者复盘裸透明窗的工程痛点（[Transparent Drag-and-Drop Shelf (Tauri)](https://dev.to/devrayat000/building-a-transparent-drag-and-drop-shelf-for-windows-with-tauri-the-engineering-war-stories-3c8c)）。

**4. macOS：Liquid Glass 时代仍须保留 traffic lights**

即便 macOS 26 Tahoe 引入了全新的 Liquid Glass 材质——一种半透明、会反射并折射周围环境的玻璃质感，覆盖工具栏、菜单、控制中心，菜单栏默认完全透明（[Apple 官方新闻稿](https://www.apple.com/lu/newsroom/2025/06/apple-introduces-a-delightful-and-elegant-new-software-design)；[MacRumors 综述](https://www.macrumors.com/roundup/macos-26/)）——macOS 人机界面准则仍硬性要求：红/黄/绿三个 traffic light 按钮"必须留在窗口左上角、保持可见且功能正常"，即便使用自定义标题栏也须保留，并明确将"用自定义实现替换标准标题栏/交通灯"列为反模式（[macOS Design Guidelines](https://lobehub.com/zh/skills/andresilvagomez-wisper-macos-design-guidelines)）。实测显示 Tahoe 中这三个按钮反而"更大了"、窗口标题左对齐、圆角更圆（[Cult of Mac](https://www.cultofmac.com/how-to/macos-26-tahoe-new-features)）。`window-vibrancy` 也已为 macOS 26+ 提供 `apply_liquid_glass` 直接调用系统玻璃（[window-vibrancy](https://github.com/tauri-apps/window-vibrancy/)）。

### 分析：完全自绘 vs 原生材质/原生标题栏

这里有一条核心取舍线。一端是"完全自绘 chrome"：视觉自由与品牌一致性最强，但开发者必须自己实现拖拽命中、三按钮、焦点态、阴影、圆角、缩放，极易踩中本项目已遇到的交互失效陷阱——无边框 + 透明 + CSS 假玻璃，既杀死系统阴影/圆角/缩放，又让标题栏拖拽与三按钮点击失效。另一端是"原生材质 + 系统标题栏机制"：零成本获得原生交互、原生质感与系统焦点态，代价是视觉定制度受系统约束。

有观点认为，多数桌面应用的稳妥选择是后者——优先用原生材质（Windows 用 Mica、macOS 用系统材质），仅通过 `ExtendsContentIntoTitleBar` / `fullSizeContentView` 这类官方机制把内容扩展到标题栏区域，而非把整条 chrome 拆掉自绘（观点，依据 [Windows 视觉层级与渲染效率](https://tsight.io/articles/16268961) 的"架构师建议"与 [Materials in Windows](https://learn.microsoft.com/EN-US/windows/apps/design/signature-experiences/materials) 的材质分工）。理由是：原生材质近乎零 GPU 开销，系统标题栏的命中、缩放、三按钮永远可用且跨版本稳定——对生产力工具尤其重要；完全自绘仅在需要极强品牌差异化的场景（创意工具、播放器等）才值得付出相应工程成本。

### 小结：本项目的可落地方向（只指路）

鉴于本项目被判定"负优化"，建议从"透明全窗 + CSS 假玻璃"回退到以下方向（仍基于 Tauri2）：
1. **保留系统窗口能力**：避免走向 `decorations:false` 的彻底无边框；若仍需自定义外观，采用"内容扩展"思路——保留系统 caption 按钮（最小化/最大化/关闭）的绘制与命中，仅把自有内容铺进标题栏区域。
2. **用真系统材质替代 CSS 假玻璃**：通过 `window-vibrancy` 的 `apply_mica`（Win11 主窗）取得真实 Mica 材质，规避 `apply_acrylic` 在拖拽/缩放时的性能劣化；阴影、圆角、缩放交给系统，不要在透明窗上自绘。
3. **材质分工**：主窗用 Mica 类静态/不透明材质（近零开销），仅浮层、右键菜单等瞬时表面考虑 Acrylic 类半透明效果。
4. **跨平台一致**：macOS 端沿用系统材质与左上角 traffic lights，不隐藏、不替换标准窗口控件；必要时用 `apply_liquid_glass`（macOS 26+）调用系统玻璃。
5. **交互正确性优先于视觉自由度**：当自定义外观与系统交互能力冲突时，优先保证拖拽、三按钮、缩放、阴影可用——这是本项目当前最该回补的能力。

---

## 结论

桌面窗口是一套由操作系统窗口管理器统一调度的交互基础设施，而非应用自绘的像素。其"可拖动、可关闭"并非应用实现的按钮逻辑，而是系统通过非客户区（NC）与命中测试（WM_NCHITTEST → HTCAPTION / HTMINBUTTON / HTMAXBUTTON / HTCLOSE）提供的系统能力（[WM_NCHITTEST 命中常量](https://www.cnblogs.com/Yzi321/p/18512010)）；标题栏与三按钮本质是系统在非客户区"预约"并垄断输入的热区（[Title bar customization](https://learn.microsoft.com/windows/apps/develop/title-bar)）。

本项目的"负优化"根因清晰且非偶发：① `decorations:false` 整体移除了系统 NC 区，原生拖拽与三按钮热区被一并摘掉，这是"功能被关"而非框架缺陷（[Tauri v2 Window Customization](https://v2.tauri.app/learn/window-customization/)）；② 无边框下命中测试须应用自管，漏标拖拽区或按钮未排除即被拖拽吞掉点击，而本项目还删掉 `data-tauri-drag-region` 改走 `startDragging` 叠加问题（[Electron Frameless Window](https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md)）；③ Tauri v2 拖拽需 capability 显式授权 `core:window:allow-start-dragging`，否则静默失败，且透明窗沦为分层窗（WS_EX_LAYERED），拿不到 DWM 原生 Mica/Acrylic 材质、阴影与 Aero Snap（[window-vibrancy](https://github.com/tauri-apps/window-vibrancy/)）。

正确方向不是"舍弃系统 chrome"，而是"在保留系统窗口能力的前提下做可控定制"：用微软 `ExtendsContentIntoTitleBar` + `SetTitleBar` 扩展内容但保留系统 caption 按钮（[WinUI 3 结构](https://learn.microsoft.com/zh-hk/windows/apps/develop/ui/windows-app-sdk-app-structure)）；材质上优先 Mica 作主窗、Acrylic 仅作浮层（[Materials in Windows](https://learn.microsoft.com/EN-US/windows/apps/design/signature-experiences/materials)）；Tauri 侧用 `window-vibrancy` 的 `apply_mica` 取得真实系统材质而非 CSS 假玻璃；macOS 端即便 Liquid Glass 时代也要保留左上角 traffic lights（[macOS Design Guidelines](https://lobehub.com/zh/skills/andresilvagomez-wisper-macos-design-guidelines)）。对多数业务型桌面应用，"原生材质 + 系统标题栏机制、仅扩展内容到标题栏"远比"彻底无边框自绘"稳妥——这正是本项目回退的方向。

---

## 参考文献

- Apple. (2025). *Apple introduces a delightful and elegant new software design*. Apple Newsroom. https://www.apple.com/lu/newsroom/2025/06/apple-introduces-a-delightful-and-elegant-new-software-design
- Cult of Mac. (2025). *Everything new in macOS 26 Tahoe*. https://www.cultofmac.com/how-to/macos-26-tahoe-new-features
- devrayat000. (2025). *Building a Transparent Drag-and-Drop Shelf for Windows with Tauri: The Engineering War Stories*. DEV Community. https://dev.to/devrayat000/building-a-transparent-drag-and-drop-shelf-for-windows-with-tauri-the-engineering-war-stories-3c8c
- Electron. (n.d.). *Frameless window*. Electron Documentation. https://github.com/electron/electron/blob/af3316707fc02a617239f54046aee08cb57e6edf/docs/api/frameless-window.md
- 2201_75395601. (2025). *Tauri v2 无边框窗口拖拽失效修复 + Windows 构建 OS error 5 拒绝访问实录*. CSDN. https://blog.csdn.net/2201_75395601/article/details/156907820
- 掘金作者. (2025). *Tauri 框架使用 Acrylic 特效拖动卡顿的解决方案*. 掘金. https://juejin.cn/post/7491240940126175284
- 社区作者. (n.d.). *Tauri 自定义 Titlebar 与窗体圆角实现指南*. https://garden.piemoe.com/dev/8zoti5ag
- MacRumors. (2025). *macOS Tahoe (macOS 26) roundup*. https://www.macrumors.com/roundup/macos-26/
- Microsoft. (n.d.-a). *About mouse input: The WM_NCHITTEST message*. Microsoft Learn. https://learn.microsoft.com/en-us/windows/win32/inputdev/about-mouse-input
- Microsoft. (n.d.-b). *DWM Performance Considerations and Best Practices*. Microsoft Learn. https://learn.microsoft.com/en-us/windows/desktop/dwm/bestpractices-ovw
- Microsoft. (n.d.-c). *Extended Window Styles*. Microsoft Learn. https://learn.microsoft.com/ja-jp/windows/win32/winmsg/extended-window-styles
- Microsoft. (n.d.-d). *Interface Styles (SDI and MDI)*. Microsoft Learn. https://learn.microsoft.com/fr-fr/previous-versions/visualstudio/visual-basic-6/aa242113(v=vs.60)
- Microsoft. (n.d.-e). *Materials used in Windows apps (Mica/Acrylic)*. Microsoft Learn. https://learn.microsoft.com/EN-US/windows/apps/design/signature-experiences/materials
- Microsoft. (n.d.-f). *SDI 和 MDI*. Microsoft Docs. https://docs.microsoft.com/zh-TW/cpp/mfc/sdi-and-mdi
- Microsoft. (n.d.-g). *Structure a modern WinUI 3 desktop app*. Microsoft Learn. https://learn.microsoft.com/zh-hk/windows/apps/develop/ui/windows-app-sdk-app-structure
- Microsoft. (n.d.-h). *The Desktop Window Manager*. Microsoft Learn. https://learn.microsoft.com/en-us/windows/win32/learnwin32/the-desktop-window-manager
- Microsoft. (n.d.-i). *Title bar customization*. Microsoft Learn. https://learn.microsoft.com/windows/apps/develop/title-bar
- Microsoft. (n.d.-j). *Window Styles*. Microsoft Learn. https://learn.microsoft.com/windows/win32/winmsg/window-styles
- Tauri. (n.d.-a). *Window Customization*. Tauri v2 Documentation. https://v2.tauri.app/learn/window-customization/
- Tauri. (n.d.-b). *自定义窗体（中文）*. https://tauri.app/zh-cn/learn/window-customization
- Tauri Apps. (n.d.). *window-vibrancy*. GitHub. https://github.com/tauri-apps/window-vibrancy/
- TrueSight. (n.d.). *Windows 视觉层级架构与渲染效率*. https://tsight.io/articles/16268961
- Yzi321. (n.d.). *WM_NCHITTEST 命中常量*. Cnblogs. https://www.cnblogs.com/Yzi321/p/18512010
- 社区作者. (n.d.). *macOS Design Guidelines (traffic lights)*. https://lobehub.com/zh/skills/andresilvagomez-wisper-macos-design-guidelines

---

> 本报告由 AI 深度研究团队（研究主编顾全之带领谭溯源/季要纲/明鉴秋/程文成/傅梓铭协作）生成，纯调研产出，**未改动任何项目代码**。重要决策请经专业人员核验；所有引用来源请在重要场景下二次核验时效性与真实性。Phase 4 框架（引言/结论/参考文献）因报告撰写人异步超时，由主理人按超时降级规则代为补全，章节正文均已经明鉴秋审稿首轮通过。
